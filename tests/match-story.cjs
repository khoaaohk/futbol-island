const fs=require('fs'),vm=require('vm'),ts=require('typescript'),assert=require('node:assert/strict');const m={exports:{}};vm.runInNewContext(ts.transpileModule(fs.readFileSync('lib/town/matchStory.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,{module:m,exports:m.exports});const {matchStory,recentMatchStories}=m.exports;const now=new Date(2026,8,14,12),item={publishedAt:new Date(2026,8,13,20).toISOString(),match:{home:'Home',away:'Away',homeScore:'1',awayScore:'0',state:'post',goals:[{player:'Player',minute:"90'+3'",team:'Home',ownGoal:false,penalty:false}],goalsComplete:true}};assert(matchStory(item,now).opener.includes('last night'));assert(matchStory(item,now).goals[0].includes("90'+3'"));assert.equal(recentMatchStories([item,{...item,match:{...item.match,state:'pre'}},{...item,publishedAt:new Date(2026,8,15).toISOString()}],now.getTime()).length,1);console.log('PASS local relative dates, stoppage time, completed-only stories and future exclusion');
