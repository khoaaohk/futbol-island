const fs=require('fs'),vm=require('vm'),ts=require('typescript'),assert=require('node:assert/strict');const m={exports:{}};vm.runInNewContext(ts.transpileModule(fs.readFileSync('lib/town/matchUpdateClock.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,{module:m,exports:m.exports});const c=m.exports.createMatchUpdateClock();let total=0,calls=0;for(let i=0;i<300;i++){const dt=c.take(1/30,false,true);total+=dt;if(dt)calls++;}assert(Math.abs(total+c.pending-10)<1e-9);assert(calls<=100);const pending=c.pending;assert.equal(c.take(10,true,false),0);assert.equal(c.pending,pending);assert(Math.abs(c.take(1/30,true,true)-pending-1/30)<1e-9);assert.equal(c.pending,0);console.log('PASS distant-match clock: elapsed time preserved, 300 frames reduced to',calls,'updates, pause/resume and immediate catch-up');
