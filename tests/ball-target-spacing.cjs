const assert=require('node:assert/strict'),fs=require('fs'),vm=require('vm'),ts=require('typescript');const m={exports:{}};vm.runInNewContext(ts.transpileModule(fs.readFileSync('lib/town/coinQuest.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,{module:m,exports:m.exports});const spots=m.exports.COIN_QUEST.filter(s=>s.wall),exceptions=new Set(['coaches|wall-right','parcel-school|school']);let min=Infinity;
for(let i=0;i<spots.length;i++)for(let j=i+1;j<spots.length;j++){const a=spots[i],b=spots[j],key=[a.id,b.id].sort().join('|');if(exceptions.has(key))continue;const distance=Math.hypot(a.wall.x-b.wall.x,a.wall.z-b.wall.z);min=Math.min(min,distance);assert(distance>=35,`${key}: only ${distance.toFixed(2)}m apart`);}
for(const s of spots){assert(Math.hypot(s.x-s.wall.x,s.z-s.wall.z)<4,'Pickup stays beside its target');if(s.wall.facing==='east'){assert(s.x>s.wall.x&&s.z===s.wall.z);assert(/east/i.test(s.clue+' '+s.detail),'East clues match '+s.id);}else assert(s.z>s.wall.z);}
assert.equal(m.exports.COIN_QUEST.length,55);console.log(`PASS ${spots.length} wall targets: minimum ${min.toFixed(2)}m separation except two approved pairs; clues and pickup sides match`);
