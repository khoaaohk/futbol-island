const fs=require('fs'),vm=require('vm'),ts=require('typescript'),T=require('three'),assert=require('node:assert/strict');const m={exports:{}};vm.runInNewContext(ts.transpileModule(fs.readFileSync('lib/graphics/hiddenTransformGate.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,{module:m,exports:m.exports,require});const scene=new T.Scene(),parent=new T.Group(),hidden=new T.Group(),mesh=new T.Mesh(new T.BoxGeometry(),new T.MeshBasicMaterial());scene.add(parent);parent.add(hidden);hidden.add(mesh);scene.updateMatrixWorld();const gate=m.exports.createHiddenTransformGate(scene);hidden.visible=false;parent.position.x=12;mesh.position.x=5;let calls=0;const update=mesh.updateMatrix;mesh.updateMatrix=function(){calls++;return update.call(this);};scene.updateMatrixWorld();assert.equal(calls,0);assert.equal(gate.stats.skipped,1);hidden.visible=true;scene.updateMatrixWorld();assert.equal(mesh.matrixWorld.elements[12],17);assert(calls>0);hidden.visible=false;mesh.position.x=8;hidden.updateMatrixWorld(true);assert.equal(mesh.matrixWorld.elements[12],20,'explicit invisible bounds/rig update remains available');gate.setEnabled(false);calls=0;scene.updateMatrixWorld();assert(calls>0);gate.dispose();assert(!Object.hasOwn(hidden,'updateMatrixWorld'));console.log('PASS hidden rendering skips, moved-parent reveal, explicit hidden updates and restoration');
