const fs=require('fs'),vm=require('vm'),ts=require('typescript'),assert=require('node:assert/strict');const m={exports:{}};vm.runInNewContext(ts.transpileModule(fs.readFileSync('lib/town/spinGesture.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,{module:m,exports:m.exports,Math});const create=m.exports.createSpinGesture;for(const direction of[1,-1]){const g=create();let fired=0;for(let i=0;i<180;i++){const a=i*Math.PI/16*direction;fired+=g.update(Math.cos(a),Math.sin(a),i*25,true)?1:0;}assert.equal(fired,1,'Five circles fires once, then cooldown');}for(const mobile of[true,false]){const g=create();let fired=0;for(let i=0;i<250;i++){const a=mobile?Math.sin(i*.2):i*.2;fired+=g.update(Math.cos(a),Math.sin(a),i*25,mobile)?1:0;}assert.equal(fired,0,'Normal reversals/desktop never trigger');}console.log('SPIN_GESTURE_PASS 4.5 turns both ways, cooldown, no wiggle or desktop triggers');
